<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <style>
        body {
            background-color: #e6f7ff; /* Light blue background */
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        #dashboard {
            background-color: #66b3ff; /* Blue color for the dashboard */
            color: white;
            padding: 20px;
            text-align: center;
        }

        #menu {
            float: left;
            width: 20%;
            padding: 20px;
            background-color: #99ccff; /* Light blue background for the menu */
            height: 100vh; /* 100% of the viewport height */
        }

        #content {
            float: left;
            width: 80%;
            padding: 20px;
        }

        .menuItem {
            margin-bottom: 10px;
            padding: 10px;
            background-color: #4d94ff; /* Darker blue for menu items */
            color: white;
            text-align: center;
            cursor: pointer;
        }

        .circularButtonsContainer {
            text-align: center;
            margin-top: 20px;
        }

        .circularButton {
            width: 100px;
            height: 100px;
            background-color: #99ccff; /* Light blue background for circular buttons */
            border-radius: 50%;
            display: inline-flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            margin: 0 10px; /* Adjust the margin as needed */
            text-decoration: none; /* Remove default link styling */
            color: black; /* Set button text color */
        }
    </style>
</head>
<body>

    <div id="dashboard">
        <h1>Dashboard Data</h1>
    </div>

    <div id="menu">
        <div id="intellilearn" class="menuItem">
            <img src="intellilearn.png" alt="Intellilearn" width="20" height="20"> Intellilearn
        </div>
        <div id="settings" class="menuItem">
            <img src="settings.png" alt="Settings" width="20" height="20"> Settings
        </div>
        <div id="help" class="menuItem">
            <img src="help.png" alt="Help" width="20" height="20"> Help
        </div>
    </div>

    <div id="content">
        <?php
        // Handle dynamic content loading here based on PHP logic
        if (isset($_GET['contentType'])) {
            $contentType = $_GET['contentType'];
            echo "<h2>{$contentType} Content Goes Here</h2>";
        }
        ?>
    </div>

    <div class="circularButtonsContainer">
        <a href="?contentType=Subjects" class="circularButton">Subjects</a>
        <a href="?contentType=Grades" class="circularButton">Grades</a>
        <a href="?contentType=Classmates" class="circularButton">Classmates</a>
        <a href="?contentType=Attendance" class="circularButton">Attendance</a>
    </div>

    <script>
        // Add JavaScript functionality here
        // For example, handle click events on circular buttons and load corresponding content in the content area.
        // You might need to use AJAX or fetch to load dynamic data.
        document.getElementById('intellilearn').addEventListener('click', function() {
            // Load Intellilearn content
            document.getElementById('content').innerHTML = '<h2>Intellilearn Content Goes Here</h2>';
        });

        document.getElementById('settings').addEventListener('click', function() {
            // Load Settings content
            document.getElementById('content').innerHTML = '<h2>Settings Content Goes Here</h2>';
        });

        document.getElementById('help').addEventListener('click', function() {
            // Load Help content
            document.getElementById('content').innerHTML = '<h2>Help Content Goes Here</h2>';
        });
    </script>

</body>
</html>
